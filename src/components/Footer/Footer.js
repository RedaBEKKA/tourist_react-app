import React from 'react';

const Footer = (props) => {
    return (
        <footer className="bg-dark text-white text-center p-3">
            @RedaBEKKA -Tout droits résérvés-
        </footer>
    );
};

export default Footer;